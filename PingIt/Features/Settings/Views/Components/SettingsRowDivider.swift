import SwiftUI

struct SettingsRowDivider: View {
    var body: some View {
        Rectangle()
            .fill(Color.pingBorder)
            .frame(height: 1)
            .padding(.leading, 18)
    }
}
